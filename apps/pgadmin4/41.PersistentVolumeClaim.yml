apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-pgadmin4
  namespace: pgadmin4
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100M
  volumeName: hp-pgadmin4
#  selector:
#    matchLabels:
#      pv-key: k8s-smb
#  storageClassName: smb-storage
